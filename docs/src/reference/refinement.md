# Refinement

In-place mesh refinement (uniform and marked h-refinement, second-order
curving) and the structured result of a refinement step.

```@docs
refine!
mark_for_refinement!
bisect!
make_second_order!
RefinementResult
refine_session!
```
